# Policy Bot Python Dependencies
# Install with: pip install -r requirements.txt
# Note: most setup is done via the Azure portal; Python is only needed
# for the scripted alternatives (configure-search.py, configure-crawler.ps1).

# Azure SDK
azure-search-documents>=11.4.0
azure-identity>=1.15.0

# Utilities
requests>=2.31.0
